## 决策记录

- **2026-03-04**: 根据用户请求，完成了 `main.tex` 的标题、作者、日期等基本元数据修改。
- **2026-03-04**: 提取了本科论文的内容作为初步输入，撰写了 `chapters/abstract.tex` 的中英文版本的摘要。
- **2026-03-04**: 创建了 `chapters/chap01_intro.tex`，包含了绪论的基本结构（背景与意义、问题定义、现有方法瓶颈、论文贡献、组织结构），并基于提取的内容进行了初步撰写。
- **2026-03-04**: 修改了 `main.tex` 以通过 `\input{chapters/abstract}` 引入摘要，并通过 `\include{chapters/chap01_intro}` 引入第一章绪论，删除了原本的测试章节。使用了 njuthesis 规范，没有进行大 diff 重排。
- **2026-03-04**: 检查了图片配置，`main.tex` 原本包含 `\config { image = { path = {./images/}, } }`，通过 njuthesis 已经保证了在包含图片时能够读取 `images/` 下的文件，所以符合需求。
- **2026-03-04**: 更新写作协议：1. 在编译约定中强制要求参考 `.vscode/settings.json` 配置输出目录到 `out/`，避免中间文件散落。2. 增加内容体量约束，要求每次生成章节内容时参考 `ZJC.pdf` 的字数和尺度，确保内容充实，避免过于单薄干瘪。
